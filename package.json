{
  "name": "dates-server",
  "version": "1.1.0",
  "description": "Backend for the Dates invitation app, with push notification reminders",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "engines": {
    "node": ">=18"
  }
}
